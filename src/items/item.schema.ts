import * as mongoose from 'mongoose';

export const ItemSchema = new mongoose.Schema({
  merchant: String
  // createdTime: Number,
  // updatedTime: Number,
  // deliveryTime: Number,
  // hashRatePrice: Number,
  // hashRatePriceDiscount: Number,
  // hashRateFinalPrice: Number,
  // electricPrice: Number,
  // electricPriceDiscount: Number,
  // electricFinalPrice: Number,
  // maintenancePrice: Number,
  // maintenancePriceDiscount: Number,
  // maintenanceFinalPrice: Number,
  // days: Number,
  // amount: Number,
  // price: Number,
  // hashRate: Number,
  // hashRateUnit: String,
  // coin: String,
  // pool: String,
  // algorithm: String,
  // soldPercent: Number,
  // miningMachine: String
});
